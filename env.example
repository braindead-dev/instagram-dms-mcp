# Instagram Gateway Address (the Go gateway must be running)
IG_GATEWAY_ADDR=http://127.0.0.1:29391

# Path to Instagram cookies JSON file (used by the Go gateway)
# If not set, the gateway will look in ~/.instagram-dms-mcp/cookies.json
IG_COOKIES_FILE=
